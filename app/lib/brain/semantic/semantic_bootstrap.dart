import 'semantic_cortex.dart';

class SemanticBootstrap {
  const SemanticBootstrap();

  void initialize(
    SemanticCortex cortex,
  ) {
    // Per ora non fa nulla.
    // Qui nascerà la conoscenza iniziale di ALMA.
  }
}